{
  "name": "web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "dev:env": "set -a && . ./.env && set +a && next dev",
    "build": "next build",
    "build:env": "set -a && . ./.env && set +a && next build",
    "start": "next start",
    "start:env": "set -a && . ./.env && set +a && next start",
    "lint": "eslint . --max-warnings=0",
    "check-types": "tsc --noEmit"
  },
  "dependencies": {
    "@av/forms": "1.0.0",
    "@av/store": "1.0.0",
    "@av/ui": "1.0.0",
    "@aws-amplify/ui-react": "^6.13.2",
    "aws-amplify": "^6.15.9",
    "next": "16.0.10",
    "next-themes": "^0.4.6",
    "react": "19.2.0",
    "react-dom": "19.2.0"
  },
  "devDependencies": {
    "@chakra-ui/cli": "^3.30.0",
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.0.1",
    "tailwindcss": "^4",
    "typescript": "^5"
  }
}
